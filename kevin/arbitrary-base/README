Write a function which returns a list of digits of a non-negative
number (first argument) in numerical system with an arbitrary base
(second argument). Digits should be represented with their integer
(base 10) values, e.g. 15 would be [1 5] in base 10, [1 1 1 1] in base
2 and [15] in base 16.
